/**
 * @description If the date-time picker should be animated. This option doesn't yet work properly.
 * @type {Boolean}
 */
const ENABLE_DATE_PICKER_ANIMATION = false;
/**
 * @description If the destruction (or removal) of the date picker should be animated.
 * @type {Boolean}
 */
const ENABLE_DATE_PICKER_DESTRUCTION_ANIMATION = false;
/**
 * @description The animation speed of the date-time picker per pixel in milliseconds.
 * @type {Number}
 */
const DATE_TIME_PICKER_ANIMATION_SPEED_PER_PIXEL = 0.25;
/**
 * @description If the date picker navigation close button should be shown.
 * @type {Boolean}
 */
const SHOW_DATE_PICKER_NAVIGATION_CLOSE_BUTTON = true;

